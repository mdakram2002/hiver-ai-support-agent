# Raw Data

This directory is intended for the raw Kaggle dataset file.

Download the `thoughtvector/customer-support-on-twitter` dataset from Kaggle and place the CSV file in this directory. The raw data is intentionally not committed to the repository.

The data pipeline scripts expect the CSV file to be provided via the `--input` parameter when running `01_clean_data.py`.